# Local setup building MicroPowerManager containers in production mode
services:
  backend-prod:
    container_name: backend-prod
    build:
      dockerfile: docker/DockerfileBackendProd
    env_file: ./dev/.env.micropowermanager-backend
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    links:
      - mysql:db
      - redis:redis
    ports:
      - 8000:80
      - 8443:443
    volumes:
      - backend_php_storage:/var/www/html/storage
    healthcheck:
      test: [CMD, curl, -f, http://localhost/up]
      start_period: 60s
      interval: 30s
      timeout: 10s
      retries: 100

  frontend-prod:
    container_name: frontend-prod
    build:
      dockerfile: docker/DockerfileFrontendProd
    env_file: ./dev/.env.micropowermanager-frontend
    ports:
      - 8001:80

  scheduler-prod:
    container_name: scheduler-prod
    build:
      dockerfile: docker/DockerfileSchedulerProd
    # micropowermanager-scheduler is a backend service and uses the same configuration as micropowermanager-backend
    env_file: ./dev/.env.micropowermanager-backend
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    links:
      - mysql:db
      - redis:redis
    volumes:
      - scheduler_php_storage:/var/www/html/storage

  queue-worker-prod:
    container_name: queue-worker-prod
    build:
      dockerfile: docker/DockerfileQueueWorkerProd
    # micropowermanager-queue-worker is a backend service and uses the same configuration as micropowermanager-backend
    env_file: ./dev/.env.micropowermanager-backend
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    links:
      - mysql:db
      - redis:redis
    volumes:
      - queue_worker_php_storage:/var/www/html/storage

  redis:
    container_name: redis
    image: redis:5
    volumes:
      - redis_data:/data
    ports:
      - 6379:6379

  mysql:
    container_name: mysql
    image: mysql:8.4
    env_file: ./dev/.env.mysql
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - 3306:3306
    healthcheck:
      test: [CMD, mysqladmin, ping, -h, localhost]
      start_period: 10s
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  mysql_data:
  redis_data:
  queue_worker_php_storage:
  scheduler_php_storage:
  backend_php_storage:
