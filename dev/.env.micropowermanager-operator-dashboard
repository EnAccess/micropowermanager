# Environment variable values used for running a local development environment
#
# DO NOT USE THESE VALUES IN PRODUCTION
#
# Operator credentials are NOT set here: the values in this file are inlined into
# the bundle at build time. They are typed into the dashboard's sign-in form and
# checked against dev/.env.micropowermanager-backend.
MPM_ENV=development
MPM_BACKEND_URL=http://localhost:8000
