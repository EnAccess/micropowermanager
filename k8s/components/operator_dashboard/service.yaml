apiVersion: v1
kind: Service
metadata:
  name: mpm-operator-dashboard-service
spec:
  selector:
    app: mpm-operator-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
