meta {
  name: Check Login (Agent App)
  type: http
  seq: 13
}

get {
  url: {{mpm_backend_url}}/api/app/me
  body: json
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.access_token: isString
}
