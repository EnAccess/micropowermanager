meta {
  name: Check Credentials
  type: http
  seq: 1
}

post {
  url: {{mpm_backend_url}}/api/spark-shs/credentials/check
  body: json
  auth: inherit
}

body:json {
  {
    "client_id": "Dummy Client ID",
    "client_secret": "Dummy Client Secret"
  }
}

assert {
  res.status: eq 200
  res.body.data: isJson
}
