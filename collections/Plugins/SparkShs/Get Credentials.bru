meta {
  name: Get Credentials
  type: http
  seq: 2
}

get {
  url: {{mpm_backend_url}}/api/spark-shs/credentials/show
  body: json
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.data: isJson
}
