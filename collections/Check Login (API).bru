meta {
  name: Check Login (API)
  type: http
  seq: 12
}

get {
  url: {{mpm_backend_url}}/api/auth/me
  body: json
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.access_token: isString
}
