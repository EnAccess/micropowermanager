name: Code Quality (Operator Dashboard)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  eslint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 20.18.1

      - name: Install Node.js dependencies
        run: npm install
        working-directory: ./src/operator-dashboard

      - name: Run ESLint
        run: npm run lint
        working-directory: ./src/operator-dashboard

  prettier:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 20.18.1

      - name: Install Node.js dependencies
        run: npm install
        working-directory: ./src/operator-dashboard

      - name: Run Prettier
        run: npm run prettier
        working-directory: ./src/operator-dashboard

  build-production:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 20.18.1

      - name: Install Node.js dependencies
        run: npm install
        working-directory: ./src/operator-dashboard

      - name: Build npm on production mode
        run: npm run build
        working-directory: ./src/operator-dashboard
